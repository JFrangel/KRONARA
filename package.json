{
  "name": "kronara-desktop",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "test": "node --test frontend-tests/*.test.js",
    "dev": "vite",
    "build": "vite build",
    "build:sidecar": "powershell -ExecutionPolicy Bypass -File scripts/build-sidecar.ps1",
    "build:all": "npm.cmd run build:sidecar && npm.cmd run build",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "latest",
    "@sveltejs/vite-plugin-svelte": "latest",
    "svelte": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@tauri-apps/cli": "latest"
  }
}
